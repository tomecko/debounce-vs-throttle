{
  "name": "debounce-throttle",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel -d 'dev' src/index.html",
    "build": "parcel build src/index.html",
    "build:prod": "rm -rf node_modules dist && yarn && yarn build && cp .htaccess dist"
  },
  "dependencies": {
    "normalize.css": "8.0.1",
    "rxjs": "6.3.3"
  },
  "devDependencies": {
    "parcel-bundler": "1.10.3",
    "sass": "1.15.2"
  }
}
